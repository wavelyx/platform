// sections
import TwoView from 'src/sections/revoke/revoke-mint-view';

// ----------------------------------------------------------------------

export const metadata = {
  title: 'wavelyz | Revoke Mint Authority (Fixed Supply)',
  description: `Ensure trust and stability for your Solana token by preventing further minting. wavelyz's Revoke Mint Authority tool keeps your Solana token supply fixed.`,
};

export default function Page() {
  return <TwoView tokenName="" />;
}
